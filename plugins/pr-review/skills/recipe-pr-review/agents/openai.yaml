interface:
  display_name: "PR Review"
  short_description: "Review GitHub PRs with configurable posting policy."
  default_prompt: "Use $recipe-pr-review to review a GitHub PR; post comments only when explicitly requested."
policy:
  allow_implicit_invocation: false
